{
	"name": "qualy-mock-api",
	"version": "1.0.0",
	"description": "JSON mock API for the Qualy frontend",
	"main": "index.js",
	"scripts": {
		"start": "json-server --watch src/db/db.json --port 1800",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Fran Beltrán",
	"dependencies": {
		"json-server": "^0.17.1"
	}
}
